{
  "name": "stackspots-events",
  "version": "1.0.0",
  "description": "Fetch, decode, and Redis-cache Stackspots contract print logs for the frontend.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test src/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@stacks/common": "^7.2.0",
    "@stacks/transactions": "^7.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "redis": "^5.8.2"
  }
}
